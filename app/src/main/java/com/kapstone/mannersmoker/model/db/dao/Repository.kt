package com.kapstone.mannersmoker.model.db.dao

import com.kapstone.mannersmoker.model.data.RetrofitInstance
import io.reactivex.Completable
import io.reactivex.Maybe
import io.reactivex.Single
import java.util.*

class Repository {
  /*  fun getDaySmokeData(day : Int, month : Int, year : Int, userId : String) : Single<List<OneSmoke>> {
        return RetrofitInstance.smokeDao.getDaySmokeData(day, month, year, userId)
    }

    fun getMonthSmokeData(month : Int, year : Int, userId : String) : Single<List<List<OneSmoke>>> {
        return RetrofitInstance.smokeDao.getMonthSmokeData(month, year, userId)
    }

    fun sendSmokeData(userId : String) : Completable {
        return RetrofitInstance.smokeDao.sendSmokeData(userId)
    } */
}